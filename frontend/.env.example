# CreatorConnect Frontend - Environment Variables

# The base URL of the backend API.
# Leave blank during local development to use Vite's built-in dev proxy.
# Set this when deploying to production (e.g., https://api.creatorconnect.com)
VITE_API_URL=
